runtime: nodejs18

entrypoint: npm start

instance_class: F1
automatic_scaling:
  min_instances: 1
  max_instances: 3 

env_variables:
  PG_USER: <postgres_user>
  PG_HOST: <cloudsql_connection>
  PG_DATABASE: <database_name>
  PG_PASSWORD: <database_password>
  PG_PORT: <database_port>
  PORT: <server_port>

  t about the instance_class?
